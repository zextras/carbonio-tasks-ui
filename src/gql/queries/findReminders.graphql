# SPDX-FileCopyrightText: 2023 Zextras <https://www.zextras.com>
#
# SPDX-License-Identifier: AGPL-3.0-only

query findReminders(
    $status: Status
) {
    findTasks(
        status: $status
    ) {
        id
        priority
        reminderAllDay
        status
        reminderAt
        title
    }
}
